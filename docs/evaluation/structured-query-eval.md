# Structured Query Evaluation

Structured query evaluation checks whether the system selected the correct table and dimensions.

Checks:

- correct table
- correct metric
- correct time range
- correct region or category
- correct aggregation
- no fabricated numbers

The first version should focus on a small set of fixed CPI tables.
