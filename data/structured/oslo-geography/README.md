# Oslo Geography Schema

This directory stores structured geography metadata for Oslo-related work.

The purpose is to keep two layers separate:

- Oslo administrative geography such as `bydel`
- SSB published area labels used in structured statistics

Do not assume these layers are directly interchangeable.
