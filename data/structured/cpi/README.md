# CPI Structured Data

Fixed CPI datasets from SSB Statbank.

Planned first files:

- `monthly-cpi.csv`
- `metadata.json`
- `ssb-query.json`

The structured query path should start narrow. Do not build broad Statbank table discovery in the first version.
